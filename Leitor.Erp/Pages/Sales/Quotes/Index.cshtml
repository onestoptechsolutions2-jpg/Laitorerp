@page
@using Microsoft.AspNetCore.Mvc.Localization
@using Leitor.Erp.Localization
@using Leitor.Erp.Entities.Sales
@inject IHtmlLocalizer<ErpResource> L
@model Leitor.Erp.Pages.Sales.Quotes.IndexModel

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["Quotes"]</h5>
        <div>
            <a asp-page="./Index" asp-page-handler="Export" asp-route-Filter="@Model.Filter" asp-route-Status="@Model.Status" class="btn btn-outline-secondary">@L["Export"]</a>
            @if (Model.CanCreate)
            {
                <a asp-page="./Create" class="btn btn-primary">@L["NewQuote"]</a>
            }
        </div>
    </div>
    <div class="card-body">
        <form method="get" class="row g-2 mb-3">
            <div class="col-auto">
                <input type="text" class="form-control" name="Filter" value="@Model.Filter" placeholder="@L["QuoteNumber"] / @L["Title"]" />
            </div>
            <div class="col-auto">
                <select name="Status" class="form-select">
                    <option value="">@L["Status"]: @L["None"]</option>
                    <option value="0" selected="@(Model.Status == QuoteStatus.Draft)">@L["Enum:QuoteStatus:0"]</option>
                    <option value="1" selected="@(Model.Status == QuoteStatus.Sent)">@L["Enum:QuoteStatus:1"]</option>
                    <option value="2" selected="@(Model.Status == QuoteStatus.Accepted)">@L["Enum:QuoteStatus:2"]</option>
                    <option value="3" selected="@(Model.Status == QuoteStatus.Rejected)">@L["Enum:QuoteStatus:3"]</option>
                    <option value="4" selected="@(Model.Status == QuoteStatus.Expired)">@L["Enum:QuoteStatus:4"]</option>
                </select>
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-secondary">@L["Search"]</button>
            </div>
        </form>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["QuoteNumber"]</th>
                        <th>@L["Title"]</th>
                        <th>@L["Customer"]</th>
                        <th>@L["Status"]</th>
                        <th>@L["Total"]</th>
                        <th>@L["Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Quotes.Any())
                    {
                        <tr class="table-empty-state"><td colspan="6">@L["NoResultsFound"]</td></tr>
                    }
                    @foreach (var quote in Model.Quotes)
                    {
                        <tr>
                            <td><a class="doc-no" asp-page="./Detail" asp-route-id="@quote.Id">@quote.QuoteNumber</a></td>
                            <td>@quote.Title</td>
                            <td>@quote.CustomerName</td>
                            <td><span class="pill @StatusPill.For(quote.Status)">@L[$"Enum:QuoteStatus:{(int)quote.Status}"]</span></td>
                            <td>@quote.Total.ToString("N2") @quote.CurrencyCode</td>
                            <td>
                                @if (Model.CanDelete)
                                {
                                    <form method="post" asp-page-handler="Delete" asp-route-id="@quote.Id" asp-route-Filter="@Model.Filter" asp-route-Status="@Model.Status" class="d-inline" data-confirm="@L["AreYouSureToDelete"]" data-confirm-variant="danger">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">@L["Delete"]</button>
                                    </form>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <partial name="/Pages/Shared/_Pagination.cshtml" model="Model.Pagination" />
    </div>
</div>
