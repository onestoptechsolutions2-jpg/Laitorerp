@page
@model Leitor.Erp.Pages.Search.IndexModel
@* Pure JSON endpoint - see Index.cshtml.cs's own comment. No markup renders on a normal GET,
   since OnGetAsync always returns a JsonResult before the page body would render. *@
