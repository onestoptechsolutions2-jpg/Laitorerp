@page "{id}"
@using Microsoft.AspNetCore.Mvc.Localization
@using Leitor.Erp.Localization
@inject IHtmlLocalizer<ErpResource> L
@model Leitor.Erp.Pages.Customers.DetailModel

<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@Model.Customer.Name</h5>
        <div>
            <a asp-page="/Accounting/Reports/CustomerStatement/Index" asp-route-CustomerId="@Model.Customer.Id" class="btn btn-outline-secondary">@L["Statement"]</a>
            @if (Model.CanEdit)
            {
                <a asp-page="./Edit" asp-route-id="@Model.Customer.Id" class="btn btn-outline-primary" data-overlay data-overlay-title="@L["EditCustomer"]">@L["Edit"]</a>
            }
            @if (Model.CanErase)
            {
                <form method="post" asp-page-handler="EraseData" class="d-inline" onsubmit="return confirm('@L["ConfirmEraseCustomerData"]');">
                    <button type="submit" class="btn btn-outline-danger">@L["EraseCustomerData"]</button>
                </form>
            }
            <a asp-page="./Index" class="btn btn-secondary">@L["BackToList"]</a>
        </div>
    </div>
    <div class="card-body">
        <partial name="/Pages/Shared/_PendingDeletionBanner.cshtml" model="Model.HasPendingDeletionRequest" />
        <dl class="row mb-0">
            <dt class="col-sm-2">@L["Email"]</dt>
            <dd class="col-sm-4">@Model.Customer.Email</dd>
            <dt class="col-sm-2">@L["PhoneNumber"]</dt>
            <dd class="col-sm-4"><partial name="/Pages/Shared/_PhoneActions.cshtml" model="Model.Customer.PhoneNumber" /></dd>

            <dt class="col-sm-2">@L["Status"]</dt>
            <dd class="col-sm-4"><span class="pill @StatusPill.For(Model.Customer.Status)">@L[$"Enum:CustomerStatus:{(int)Model.Customer.Status}"]</span></dd>
            <dt class="col-sm-2">@L["Country"]</dt>
            <dd class="col-sm-4">@Model.Customer.Country</dd>

            <dt class="col-sm-2">@L["AccountOwner"]</dt>
            <dd class="col-sm-10">@(Model.Customer.AccountOwnerUserName ?? L["None"].Value)</dd>

            @if (!string.IsNullOrWhiteSpace(Model.Customer.PortalUserName))
            {
                <dt class="col-sm-2">@L["PortalAccess"]</dt>
                <dd class="col-sm-10">@Model.Customer.PortalUserName</dd>
            }

            <dt class="col-sm-2">@L["AddressLine"]</dt>
            <dd class="col-sm-10">
                @Model.Customer.AddressLine, @Model.Customer.City @Model.Customer.State @Model.Customer.PostalCode
            </dd>

            @if (!string.IsNullOrWhiteSpace(Model.Customer.Notes))
            {
                <dt class="col-sm-2">@L["Notes"]</dt>
                <dd class="col-sm-10">@Model.Customer.Notes</dd>
            }
        </dl>
    </div>
</div>

<div class="card mb-3">
    <div class="card-header">
        <h5 class="card-title mb-0">@L["PipelineAndFinance"]</h5>
    </div>
    <div class="card-body">
        <div class="row mb-3">
            <div class="col-xl-4 col-lg-6 col-md-6 col-sm-6 d-flex mb-3">
                <div class="card w-100 kpi-card">
                    <div class="card-body">
                        <div class="kpi-icon"><i class="fas fa-sack-dollar"></i></div>
                        <div class="kpi-body">
                            <div class="text-muted small">@L["LifetimeRevenue"]</div>
                            <div class="display-6">@Model.LifetimeRevenue.ToString("N2")</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-xl-4 col-lg-6 col-md-6 col-sm-6 d-flex mb-3">
                <div class="card w-100 kpi-card">
                    <div class="card-body">
                        <div class="kpi-icon"><i class="fas fa-hand-holding-dollar"></i></div>
                        <div class="kpi-body">
                            <div class="text-muted small">@L["OutstandingBalance"]</div>
                            <div class="display-6">@Model.OutstandingBalance.ToString("N2")</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-xl-4 col-lg-6 col-md-6 col-sm-6 d-flex mb-3">
                <div class="card w-100 kpi-card">
                    <div class="card-body">
                        <div class="kpi-icon"><i class="fas fa-file-invoice-dollar"></i></div>
                        <div class="kpi-body">
                            <div class="text-muted small">@L["TotalOrders"]</div>
                            <div class="display-6">@Model.TotalOrders</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-xl-4 col-lg-6 col-md-6 col-sm-6 d-flex mb-3">
                <div class="card w-100 kpi-card">
                    <div class="card-body">
                        <div class="kpi-icon"><i class="fas fa-bullseye"></i></div>
                        <div class="kpi-body">
                            <div class="text-muted small">@L["OpenOpportunities"]</div>
                            <div class="display-6">@Model.OpenOpportunities</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-xl-4 col-lg-6 col-md-6 col-sm-6 d-flex mb-3">
                <div class="card w-100 kpi-card">
                    <div class="card-body">
                        <div class="kpi-icon"><i class="fas fa-percent"></i></div>
                        <div class="kpi-body">
                            <div class="text-muted small">@L["WinRate"]</div>
                            <div class="display-6">@(Model.WinRate.HasValue ? (Model.WinRate.Value * 100).ToString("N0") + "%" : "-")</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-xl-4 col-lg-6 col-md-6 col-sm-6 d-flex mb-3">
                <div class="card w-100 kpi-card">
                    <div class="card-body">
                        <div class="kpi-icon"><i class="fas fa-scale-balanced"></i></div>
                        <div class="kpi-body">
                            <div class="text-muted small">@L["AverageDealSize"]</div>
                            <div class="display-6">@(Model.AverageDealSize.HasValue ? Model.AverageDealSize.Value.ToString("N2") : "-")</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @if (Model.OriginatingLead != null)
        {
            <dl class="row mb-3">
                <dt class="col-sm-2">@L["FromLead"]</dt>
                <dd class="col-sm-10">@Model.OriginatingLead.Name (@L[$"Enum:LeadSource:{(int)Model.OriginatingLead.Source}"])</dd>
            </dl>

            @if (Model.LeadTouches.Count > 0)
            {
                <h6>@L["LeadActivity"]</h6>
                <div class="mb-3">
                    @foreach (var touch in Model.LeadTouches)
                    {
                        <div class="border-bottom pb-2 mb-2">
                            <div>
                                <span class="badge bg-secondary">@L[$"Enum:LeadChannel:{(int)touch.Channel}"]</span>
                                <span class="badge bg-info">@L[$"Enum:LeadDirection:{(int)touch.Direction}"]</span>
                                <strong class="ms-2">@touch.CreatorUserName</strong>
                                <span class="text-muted ms-2">@touch.TouchedAt.ToString("g")</span>
                            </div>
                            @if (!string.IsNullOrWhiteSpace(touch.Notes))
                            {
                                <div>@touch.Notes</div>
                            }
                        </div>
                    }
                </div>
            }
        }

        <h6>@L["Opportunities"]</h6>
        @if (Model.Opportunities.Count == 0)
        {
            <p class="text-muted small">@L["NoneYet"]</p>
        }
        else
        {
            <div class="table-responsive mb-3">
                <table class="table table-sm table-striped">
                    <thead><tr><th>@L["Name"]</th><th>@L["Status"]</th><th>@L["EstimatedValue"]</th></tr></thead>
                    <tbody>
                        @foreach (var opportunity in Model.Opportunities)
                        {
                            <tr>
                                <td><a asp-page="/Opportunities/Detail" asp-route-id="@opportunity.Id">@opportunity.Name</a></td>
                                <td><span class="pill @StatusPill.For(opportunity.Status)">@L[$"Enum:OpportunityStatus:{(int)opportunity.Status}"]</span></td>
                                <td>@opportunity.EstimatedValue?.ToString("N2")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

        <h6>@L["Proposals"]</h6>
        @if (Model.Proposals.Count == 0)
        {
            <p class="text-muted small">@L["NoneYet"]</p>
        }
        else
        {
            <div class="table-responsive mb-3">
                <table class="table table-sm table-striped">
                    <thead><tr><th>@L["ProposalNumber"]</th><th>@L["Title"]</th><th>@L["Status"]</th></tr></thead>
                    <tbody>
                        @foreach (var proposal in Model.Proposals)
                        {
                            <tr>
                                <td><a class="doc-no" asp-page="/Opportunities/Proposals/Edit" asp-route-id="@proposal.Id" asp-route-opportunityId="@proposal.OpportunityId">@proposal.ProposalNumber</a></td>
                                <td>@proposal.Title</td>
                                <td><span class="pill @StatusPill.For(proposal.Status)">@L[$"Enum:ProposalStatus:{(int)proposal.Status}"]</span></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

        <h6>@L["Quotes"]</h6>
        @if (Model.Quotes.Count == 0)
        {
            <p class="text-muted small">@L["NoneYet"]</p>
        }
        else
        {
            <div class="table-responsive mb-3">
                <table class="table table-sm table-striped">
                    <thead><tr><th>@L["QuoteNumber"]</th><th>@L["Status"]</th><th>@L["Total"]</th></tr></thead>
                    <tbody>
                        @foreach (var quote in Model.Quotes)
                        {
                            <tr>
                                <td><a class="doc-no" asp-page="/Sales/Quotes/Detail" asp-route-id="@quote.Id">@quote.QuoteNumber</a></td>
                                <td><span class="pill @StatusPill.For(quote.Status)">@L[$"Enum:QuoteStatus:{(int)quote.Status}"]</span></td>
                                <td>@quote.Total.ToString("N2")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

        <h6>@L["Orders"]</h6>
        @if (Model.Orders.Count == 0)
        {
            <p class="text-muted small">@L["NoneYet"]</p>
        }
        else
        {
            <div class="table-responsive mb-3">
                <table class="table table-sm table-striped">
                    <thead><tr><th>@L["OrderNumber"]</th><th>@L["Status"]</th><th>@L["Total"]</th></tr></thead>
                    <tbody>
                        @foreach (var order in Model.Orders)
                        {
                            <tr>
                                <td><a class="doc-no" asp-page="/Sales/Orders/Detail" asp-route-id="@order.Id">@order.OrderNumber</a></td>
                                <td><span class="pill @StatusPill.For(order.Status)">@L[$"Enum:OrderStatus:{(int)order.Status}"]</span></td>
                                <td>@order.Total.ToString("N2")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

        <h6>@L["Invoices"]</h6>
        @if (Model.Invoices.Count == 0)
        {
            <p class="text-muted small mb-0">@L["NoneYet"]</p>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-sm table-striped mb-0">
                    <thead><tr><th>@L["InvoiceNumber"]</th><th>@L["PaymentStatus"]</th><th>@L["Total"]</th><th>@L["AmountPaid"]</th></tr></thead>
                    <tbody>
                        @foreach (var invoice in Model.Invoices)
                        {
                            <tr>
                                <td><a class="doc-no" asp-page="/Sales/Invoices/Detail" asp-route-id="@invoice.Id">@invoice.InvoiceNumber</a></td>
                                <td><span class="pill @StatusPill.For(invoice.PaymentStatus)">@L[$"Enum:InvoicePaymentStatus:{(int)invoice.PaymentStatus}"]</span></td>
                                <td>@invoice.Total.ToString("N2")</td>
                                <td>@invoice.AmountPaid.ToString("N2")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>

<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">Price Lists</h5>
        <form method="post" asp-page-handler="AddPriceList" class="d-inline">
            <select name="priceListId" class="form-select form-select-sm d-inline" style="width: auto;">
                <option value="">Select a price list to add...</option>
                @foreach (var pl in Model.AvailablePriceLists)
                {
                    <option value="@pl.Id">@pl.Name</option>
                }
            </select>
            <button type="submit" class="btn btn-primary btn-sm">Add</button>
        </form>
    </div>
    <div class="card-body">
        @if (Model.CustomerPriceLists.Count == 0)
        {
            <p class="text-muted small">No price lists assigned. Proposals will use product base prices.</p>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Price List</th>
                            <th>Primary</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var cpl in Model.CustomerPriceLists)
                        {
                            <tr>
                                <td>@cpl.PriceListName</td>
                                <td>
                                    @if (cpl.IsPrimary)
                                    {
                                        <span class="badge bg-success">✓ Primary</span>
                                    }
                                    else
                                    {
                                        <form method="post" asp-page-handler="SetPrimary" asp-route-priceListId="@cpl.Id" class="d-inline">
                                            <button type="submit" class="btn btn-sm btn-outline-secondary">Set as Primary</button>
                                        </form>
                                    }
                                </td>
                                <td>
                                    <form method="post" asp-page-handler="RemovePriceList" asp-route-priceListId="@cpl.Id" class="d-inline" onsubmit="return confirm('Remove this price list?');">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">Remove</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>

<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["Contacts"]</h5>
        <a asp-page="/Customers/Contacts/Create" asp-route-customerId="@Model.Customer.Id" class="btn btn-primary btn-sm" data-overlay data-overlay-title="@L["NewContact"]">@L["NewContact"]</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["FullName"]</th>
                        <th>@L["JobTitle"]</th>
                        <th>@L["Email"]</th>
                        <th>@L["PhoneNumber"]</th>
                        <th>@L["IsPrimary"]</th>
                        <th>@L["Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var contact in Model.Contacts)
                    {
                        <tr>
                            <td>@contact.FullName</td>
                            <td>@contact.JobTitle</td>
                            <td>@contact.Email</td>
                            <td><partial name="/Pages/Shared/_PhoneActions.cshtml" model="contact.PhoneNumber" /></td>
                            <td>@(contact.IsPrimary ? "✓" : "")</td>
                            <td>
                                <a asp-page="/Customers/Contacts/Edit" asp-route-id="@contact.Id" asp-route-customerId="@Model.Customer.Id" class="btn btn-sm btn-outline-primary" data-overlay data-overlay-title="@L["EditContact"]">@L["Edit"]</a>
                                <form method="post" asp-page-handler="DeleteContact" asp-route-contactId="@contact.Id" class="d-inline" onsubmit="return confirm('@L["AreYouSureToDelete"]');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">@L["Delete"]</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["Contracts"]</h5>
        <a asp-page="/Customers/Contracts/Create" asp-route-customerId="@Model.Customer.Id" class="btn btn-primary btn-sm" data-overlay data-overlay-title="@L["NewContract"]">@L["NewContract"]</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["ContractNumber"]</th>
                        <th>@L["Title"]</th>
                        <th>@L["Type"]</th>
                        <th>@L["Status"]</th>
                        <th>@L["StartDate"]</th>
                        <th>@L["EndDate"]</th>
                        <th>@L["Value"]</th>
                        <th>@L["ServicesIncluded"]</th>
                        <th>@L["Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var contract in Model.Contracts)
                    {
                        <tr>
                            <td>@contract.ContractNumber</td>
                            <td>@contract.Title</td>
                            <td>@L[$"Enum:CustomerContractType:{(int)contract.Type}"]</td>
                            <td>@L[$"Enum:CustomerContractStatus:{(int)contract.Status}"]</td>
                            <td>@contract.StartDate.ToShortDateString()</td>
                            <td>@(contract.EndDate?.ToShortDateString())</td>
                            <td>@contract.Value</td>
                            <td>
                                @{
                                    var includedServices = Leitor.Erp.Entities.Customers.ContractServiceScopeOptions.All.Where(flag => contract.ServicesIncluded.HasFlag(flag)).ToList();
                                }
                                @if (includedServices.Count == 0)
                                {
                                    <span class="text-muted">&mdash;</span>
                                }
                                else
                                {
                                    <span title="@string.Join(", ", includedServices.Select(flag => L[$"Enum:ContractServiceScope:{flag}"].Value))">@includedServices.Count @L["ServicesCount"]</span>
                                }
                            </td>
                            <td>
                                @if (contract.ContractTemplateId.HasValue)
                                {
                                    <a asp-page="./Detail" asp-route-id="@Model.Customer.Id" asp-page-handler="ContractPdf" asp-route-contractId="@contract.Id" class="btn btn-sm btn-outline-secondary" target="_blank">@L["Pdf"]</a>
                                }
                                <a asp-page="/Customers/Contracts/Edit" asp-route-id="@contract.Id" asp-route-customerId="@Model.Customer.Id" class="btn btn-sm btn-outline-primary" data-overlay data-overlay-title="@L["EditContract"]">@L["Edit"]</a>
                                <form method="post" asp-page-handler="DeleteContract" asp-route-contractId="@contract.Id" class="d-inline" onsubmit="return confirm('@L["AreYouSureToDelete"]');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">@L["Delete"]</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["Tasks"]</h5>
        <a asp-page="/Customers/Tasks/Create" asp-route-customerId="@Model.Customer.Id" class="btn btn-primary btn-sm" data-overlay data-overlay-title="@L["NewTask"]">@L["NewTask"]</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["Title"]</th>
                        <th>@L["DueDate"]</th>
                        <th>@L["AssignedTo"]</th>
                        <th>@L["IsCompleted"]</th>
                        <th>@L["Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var task in Model.TaskItems)
                    {
                        var duePillClass = TaskDueStatus.PillClass(task.DueDate, task.IsCompleted, DateTime.Now, Model.TaskDueSoonLeadDays);
                        <tr class="@(task.IsCompleted ? "text-decoration-line-through text-muted" : "")">
                            <td>@task.Title</td>
                            <td>
                                @(task.DueDate?.ToShortDateString())
                                @if (!string.IsNullOrEmpty(duePillClass))
                                {
                                    <span class="pill @duePillClass">@(duePillClass == "pill-danger" ? L["Overdue"] : L["DueSoon"])</span>
                                }
                            </td>
                            <td>@task.AssignedToUserName</td>
                            <td>@(task.IsCompleted ? "✓" : "")</td>
                            <td>
                                <form method="post" asp-page-handler="ToggleTask" asp-route-taskId="@task.Id" class="d-inline">
                                    <button type="submit" class="btn btn-sm btn-outline-secondary">@(task.IsCompleted ? L["Reopen"] : L["MarkComplete"])</button>
                                </form>
                                <a asp-page="/Customers/Tasks/Edit" asp-route-id="@task.Id" asp-route-customerId="@Model.Customer.Id" class="btn btn-sm btn-outline-primary" data-overlay data-overlay-title="@L["EditTask"]">@L["Edit"]</a>
                                <form method="post" asp-page-handler="DeleteTask" asp-route-taskId="@task.Id" class="d-inline" onsubmit="return confirm('@L["AreYouSureToDelete"]');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">@L["Delete"]</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["Documents"]</h5>
        <a asp-page="/Customers/Attachments/Upload" asp-route-customerId="@Model.Customer.Id" class="btn btn-primary btn-sm">@L["UploadDocument"]</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["FileName"]</th>
                        <th>@L["FileSize"]</th>
                        <th>@L["UploadedBy"]</th>
                        <th>@L["Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var attachment in Model.Attachments)
                    {
                        <tr>
                            <td>@attachment.FileName</td>
                            <td>@((attachment.FileSizeBytes / 1024.0).ToString("N0")) KB</td>
                            <td>@attachment.UploadedByUserName</td>
                            <td>
                                <a asp-page="/Customers/Attachments/Download" asp-route-id="@attachment.Id" class="btn btn-sm btn-outline-primary">@L["Download"]</a>
                                <form method="post" asp-page-handler="DeleteAttachment" asp-route-attachmentId="@attachment.Id" class="d-inline" onsubmit="return confirm('@L["AreYouSureToDelete"]');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">@L["Delete"]</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["Activity"]</h5>
        <partial name="/Pages/Shared/_PhoneActions.cshtml" model="Model.Customer.PhoneNumber" />
    </div>
    <div class="card-body">
        <form method="post" asp-page-handler="AddNote" class="row g-2 mb-3">
            <div class="col-auto">
                <select asp-for="NewNote.Type" class="form-select">
                    <option value="0">@L["Enum:CustomerNoteType:0"]</option>
                    <option value="1">@L["Enum:CustomerNoteType:1"]</option>
                    <option value="2">@L["Enum:CustomerNoteType:2"]</option>
                    <option value="3">@L["Enum:CustomerNoteType:3"]</option>
                </select>
            </div>
            <div class="col-auto">
                <select asp-for="NewNote.Direction" class="form-select">
                    <option value="0">@L["Enum:LeadDirection:0"]</option>
                    <option value="1">@L["Enum:LeadDirection:1"]</option>
                </select>
            </div>
            <div class="col">
                <input type="text" asp-for="NewNote.Text" class="form-control" placeholder="@L["NoteText"]" />
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-primary">@L["AddNote"]</button>
            </div>
        </form>

        @foreach (var note in Model.Notes)
        {
            <div class="border-bottom pb-2 mb-2">
                <div class="d-flex justify-content-between">
                    <div>
                        <span class="badge bg-secondary">@L[$"Enum:CustomerNoteType:{(int)note.Type}"]</span>
                        <span class="badge bg-info">@L[$"Enum:LeadDirection:{(int)note.Direction}"]</span>
                        <strong class="ms-2">@note.CreatorUserName</strong>
                        <span class="text-muted ms-2">@note.TouchedAt.ToString("g")</span>
                    </div>
                    <form method="post" asp-page-handler="DeleteNote" asp-route-noteId="@note.Id" onsubmit="return confirm('@L["AreYouSureToDelete"]');">
                        <button type="submit" class="btn btn-sm btn-outline-danger">@L["Delete"]</button>
                    </form>
                </div>
                <div>@note.Text</div>
            </div>
        }
    </div>
</div>

<div class="card mt-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["FieldServiceJobs"]</h5>
        <a asp-page="/FieldService/Jobs/Create" class="btn btn-primary btn-sm">@L["NewJob"]</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["Type"]</th>
                        <th>@L["Status"]</th>
                        <th>@L["ScheduledDate"]</th>
                        <th>@L["AssignedTo"]</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var job in Model.FieldServiceJobs)
                    {
                        <tr>
                            <td><a asp-page="/FieldService/Jobs/Detail" asp-route-id="@job.Id">@L[$"Enum:FieldServiceJobType:{(int)job.Type}"]</a></td>
                            <td><span class="pill @StatusPill.For(job.Status)">@L[$"Enum:FieldServiceJobStatus:{(int)job.Status}"]</span></td>
                            <td>@job.ScheduledDate.ToShortDateString()</td>
                            <td>@job.AssignedToUserName</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="card mt-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["Tickets"]</h5>
        <div>
            <a asp-page="/Support/WarrantyClaims/Create" asp-route-customerId="@Model.Customer.Id" class="btn btn-outline-primary btn-sm">@L["NewWarrantyClaim"]</a>
            <a asp-page="/Support/Tickets/Create" class="btn btn-primary btn-sm">@L["NewTicket"]</a>
        </div>
    </div>
    <div class="card-body">
        @if (Model.SlaPerformance is { TotalCount: > 0 })
        {
            <p class="text-muted mb-3">
                @L["SlaPerformance"]:
                <span class="pill @(Model.SlaPerformance.CompliancePercent >= 90 ? "pill-success" : Model.SlaPerformance.CompliancePercent >= 75 ? "pill-warning" : "pill-danger")">
                    @* CompliancePercent is only null when TotalCount is 0 (see
                       SupportAnalyticsAppService.GetCustomerSlaPerformanceAsync) - the
                       TotalCount: > 0 check above guarantees it's set here, just not in a way
                       the compiler's null-flow analysis can see across two properties. *@
                    @Model.SlaPerformance.CompliancePercent!.Value.ToString("N1")%
                </span>
                (@Model.SlaPerformance.MetCount @L["Of"] @Model.SlaPerformance.TotalCount @L["TicketsMetSla"])
            </p>
        }
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["TicketNumber"]</th>
                        <th>@L["Subject"]</th>
                        <th>@L["Status"]</th>
                        <th>@L["Priority"]</th>
                        <th>@L["AssignedTo"]</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var ticket in Model.Tickets)
                    {
                        <tr>
                            <td><a class="doc-no" asp-page="/Support/Tickets/Detail" asp-route-id="@ticket.Id">@ticket.TicketNumber</a></td>
                            <td>@ticket.Subject</td>
                            <td><span class="pill @StatusPill.For(ticket.Status)">@L[$"Enum:TicketStatus:{(int)ticket.Status}"]</span></td>
                            <td>@L[$"Enum:TicketPriority:{(int)ticket.Priority}"]</td>
                            <td>@ticket.AssignedToUserName</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@if (Model.Projects.Any())
{
    <div class="card mt-3">
        <div class="card-header">
            <h5 class="card-title mb-0">@L["Projects"]</h5>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>@L["ProjectNumber"]</th>
                            <th>@L["Title"]</th>
                            <th>@L["Status"]</th>
                            <th>@L["DependsOnProject"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var project in Model.Projects)
                        {
                            <tr>
                                <td><a class="doc-no" asp-page="/Projects/Detail" asp-route-id="@project.Id">@project.ProjectNumber</a></td>
                                <td>@project.Title</td>
                                <td><span class="pill @StatusPill.For(project.Status)">@L[$"Enum:ProjectStatus:{(int)project.Status}"]</span></td>
                                <td>
                                    @if (project.DependsOnProjectId.HasValue)
                                    {
                                        <a asp-page="/Projects/Detail" asp-route-id="@project.DependsOnProjectId">@project.DependsOnProjectTitle</a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}

@if (Model.ServiceRequests.Any())
{
    <div class="card mt-3">
        <div class="card-header">
            <h5 class="card-title mb-0">@L["ServiceRequests"]</h5>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>@L["RequestNumber"]</th>
                            <th>@L["Description"]</th>
                            <th>@L["Status"]</th>
                            <th>@L["RequestedDate"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var request in Model.ServiceRequests)
                        {
                            <tr>
                                <td><a class="doc-no" asp-page="/ServiceRequests/Detail" asp-route-id="@request.Id">@request.RequestNumber</a></td>
                                <td>@request.Description</td>
                                <td><span class="pill @StatusPill.For(request.Status)">@L[$"Enum:ServiceRequestStatus:{(int)request.Status}"]</span></td>
                                <td>@request.RequestedDate.ToShortDateString()</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
