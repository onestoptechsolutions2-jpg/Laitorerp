@page
@using Microsoft.AspNetCore.Mvc.Localization
@using Leitor.Erp.Localization
@inject IHtmlLocalizer<ErpResource> L
@model Leitor.Erp.Pages.Projects.CreateModel

<div class="card">
    <div class="card-header">
        <h5 class="card-title mb-0">@L["NewProject"]</h5>
    </div>
    <div class="card-body">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <abp-select asp-for="Project.CustomerId" asp-items="Model.CustomerOptions" label="@L["Customer"].Value" />
            <abp-input asp-for="Project.Title" />
            <abp-input asp-for="Project.Description" />
            <abp-input asp-for="Project.Status" />
            <abp-input asp-for="Project.StartDate" />
            <abp-input asp-for="Project.EndDate" />
            <abp-input asp-for="Project.Budget" />

            <div class="mt-3">
                <button type="submit" class="btn btn-primary">@L["Save"]</button>
                <a asp-page="./Index" class="btn btn-secondary">@L["Cancel"]</a>
            </div>
        </form>
    </div>
</div>
