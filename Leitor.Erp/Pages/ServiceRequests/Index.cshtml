@page
@using Microsoft.AspNetCore.Mvc.Localization
@using Leitor.Erp.Localization
@inject IHtmlLocalizer<ErpResource> L
@model Leitor.Erp.Pages.ServiceRequests.IndexModel

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">@L["ServiceRequests"]</h5>
        @if (Model.CanCreate)
        {
            <a asp-page="./Create" class="btn btn-primary" data-overlay data-overlay-title="@L["NewServiceRequest"]">@L["NewServiceRequest"]</a>
        }
    </div>
    <div class="card-body">
        <form method="get" class="row g-2 mb-3">
            <div class="col-auto">
                <input type="text" class="form-control" name="Filter" value="@Model.Filter" placeholder="@L["Description"]" />
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-secondary">@L["Search"]</button>
            </div>
        </form>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>@L["RequestNumber"]</th>
                        <th>@L["Customer"]</th>
                        <th>@L["Status"]</th>
                        <th>@L["RequestedDate"]</th>
                        <th>@L["Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.ServiceRequests.Any())
                    {
                        <tr class="table-empty-state">
                            <td colspan="5">
                                <div>@L["NoResultsFound"]</div>
                                @if (Model.CanCreate)
                                {
                                    <a asp-page="./Create" class="btn btn-sm btn-outline-primary mt-2" data-overlay data-overlay-title="@L["NewServiceRequest"]">@L["NewServiceRequest"]</a>
                                }
                            </td>
                        </tr>
                    }
                    @foreach (var request in Model.ServiceRequests)
                    {
                        <tr>
                            <td><a class="doc-no" asp-page="./Detail" asp-route-id="@request.Id">@request.RequestNumber</a></td>
                            <td>@request.CustomerName</td>
                            <td><span class="pill @StatusPill.For(request.Status)">@L[$"Enum:ServiceRequestStatus:{(int)request.Status}"]</span></td>
                            <td>@request.RequestedDate.ToShortDateString()</td>
                            <td>
                                @if (Model.CanDelete)
                                {
                                    <form method="post" asp-page-handler="Delete" asp-route-id="@request.Id" asp-route-Filter="@Model.Filter" class="d-inline" data-confirm="@(Model.CanDecideDeletions ? L["AreYouSureToDelete"] : L["AreYouSureToRequestDeletion"])" data-confirm-variant="danger">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">@(Model.CanDecideDeletions ? L["Delete"] : L["RequestDeletion"])</button>
                                    </form>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <partial name="/Pages/Shared/_Pagination.cshtml" model="Model.Pagination" />
    </div>
</div>
