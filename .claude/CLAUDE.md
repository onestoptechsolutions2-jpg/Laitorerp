# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

# erp-review
- **erp-review** (`.claude/skills/erp-review/SKILL.md`) - the 2026-08-22 UI/UX and product-gap review backlog (Calendar/Leads save bug, Quotes overlay/actions-menu/Confirm bug, WhatsApp/email share + document links, duplicate Categories, Identity/HR + Cybersecurity module overlap, Ticket/ServiceRequest/WarrantyClaim duplication, Field Service, sidebar redundancy, My Workspace layout). Trigger: `/erp-review`
When the user types `/erp-review`, use the installed erp-review skill before doing anything else.
